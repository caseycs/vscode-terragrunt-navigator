# Project Instructions

## Version Bumping

When bumping a version tag, always update `version` in `package.json` to match the tag before committing and tagging. After creating a git tag, always run `npm run package` to build the `.vsix` file. 
