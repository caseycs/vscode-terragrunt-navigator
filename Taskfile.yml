version: '3'

tasks:
  build:
    desc: Build the extension with esbuild
    cmds:
      - node esbuild.js

  package:
    desc: Package the extension as a .vsix file
    deps: [build]
    cmds:
      - npx @vscode/vsce package
